# ee/ — Enterprise Edition (placeholder)

Enterprise features — hosted console, team memory server, SSO/RBAC, SIEM
export — will live in this directory under a **commercial license**.

Nothing here yet.

**Everything outside `ee/` is and will remain Apache-2.0** (see the repository
[LICENSE](../LICENSE)). Code inside `ee/` is NOT covered by the Apache-2.0
grant; it will carry its own license header and terms when it lands.
