<?xml version="1.0" encoding="UTF-8"?>
<!--
  crag Anchor daemon — launchd agent (macOS).

  Install (pip path):
    git clone <repo> ~/crag-anchor && cd ~/crag-anchor
    python3 -m venv .venv && source .venv/bin/activate
    pip install -e '.[embeddings]'
    # edit the paths below to match your checkout + username, then:
    cp packaging/launchd/sh.crag.anchor.plist ~/Library/LaunchAgents/
    launchctl load ~/Library/LaunchAgents/sh.crag.anchor.plist
    curl -sf http://127.0.0.1:8786/health

  Data (DB + logs) defaults to the checkout unless CRAG_ANCHOR_HOME is set below.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>sh.crag.anchor</string>

    <!-- EDIT: absolute path to the venv's crag-anchor console script. -->
    <key>ProgramArguments</key>
    <array>
        <string>/Users/CHANGE_ME/crag-anchor/.venv/bin/crag-anchor</string>
    </array>

    <!-- EDIT: your checkout root. -->
    <key>WorkingDirectory</key>
    <string>/Users/CHANGE_ME/crag-anchor</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>CRAG_ANCHOR_HOME</key>
        <string>/Users/CHANGE_ME/crag-anchor</string>
        <key>CRAG_ANCHOR_DAEMON_HOST</key>
        <string>127.0.0.1</string>
        <key>CRAG_ANCHOR_DAEMON_PORT</key>
        <string>8786</string>
        <!-- Team/server grounding mode (optional):
        <key>CRAG_ANCHOR_GROUNDING_PROVIDER</key>
        <string>anthropic-api</string>
        <key>ANTHROPIC_API_KEY</key>
        <string>sk-ant-...</string>
        -->
    </dict>

    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>

    <!-- EDIT: log paths (or point at CRAG_ANCHOR_HOME/logs/). -->
    <key>StandardOutPath</key>
    <string>/Users/CHANGE_ME/crag-anchor/logs/launchd-stdout.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/CHANGE_ME/crag-anchor/logs/launchd-stderr.log</string>
</dict>
</plist>
