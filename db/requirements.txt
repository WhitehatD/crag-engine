# crag-engine Python dependencies
# Install: pip install -r db/requirements.txt
#
# Core (usually pre-installed via pip):
numpy>=2.0
onnxruntime>=1.24

# Semantic search (Phase 2):
fastembed>=0.8.0

# MCP server (Phase 3):
mcp>=1.0

# Daemon HTTP service (Phase 5):
fastapi>=0.115
uvicorn>=0.31
httpx>=0.27
